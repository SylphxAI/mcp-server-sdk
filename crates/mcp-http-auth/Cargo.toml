[package]
name = "mcp-http-auth"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "HTTP transport auth pure product kernels (parity with src/transports/auth.ts)"

[lints]
workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
