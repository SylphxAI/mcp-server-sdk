[package]
name = "mcp-notifications"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Notification product emitter kernels (parity with src/notifications/)"

[lints]
workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
mcp-protocol = { path = "../mcp-protocol" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
