[package]
name = "mcp-pagination"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Pure cursor-based pagination helpers for MCP list operations"

[lints]
workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
