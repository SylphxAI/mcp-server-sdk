[package]
name = "mcp-stdio"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Stdio NDJSON framing product kernels (parity with src/transports/stdio.ts + src/app/stdio.ts)"

[lints]
workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
mcp-jsonrpc = { path = "../mcp-jsonrpc" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
