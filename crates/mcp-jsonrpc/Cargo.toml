[package]
name = "mcp-jsonrpc"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Pure JSON-RPC 2.0 parse/construct (BW3 pure residual)"

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
