[package]
name = "mcp-builders"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Tool/resource/prompt product builders (parity with src/builders/*)"

[lints]
workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
mcp-protocol = { path = "../mcp-protocol" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
